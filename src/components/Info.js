import React from "react";
import styled from "styled-components";

function Info() {
  return (
    <Wrapper>
      <div>Info comp</div>
    </Wrapper>
  );
}

const Wrapper = styled.section`
  font-size: 2rem;
  color: var(--col-1);
`;

export default Info;
